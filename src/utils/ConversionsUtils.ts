export function gigabytesToBytes(gigas: number): number {
    return gigas * 1024 * 1024 * 1000;
}

export function gigabytesToKilobytes(gigas: number): number {
    return gigas * 1024 * 1024;
}
